/// <summary>
/// Describes what the avatar should do after it finishes walking to its target.
/// </summary>
public enum AvatarAction
{
    /// <summary>Simply walk to the destination, then idle.</summary>
    Walk,

    /// <summary>Walk to the destination, then play the watering animation.</summary>
    WalkAndWater
}
